# 物語時系列の例

- 状態：Draft
- 仕様バージョン：0.1

## 目的

この文書は、仕様上の境界を確認する最小例を保存します。
例は実データと区別し、未確認の内容を事実として追加しません。

## 予定する例

- 確定した単日出来事。
- 確定した継続期間。
- 期間内のいずれか1日に発生する出来事。
- 複数のStoryBlockが根拠になる出来事。
- 同じ共通出来事を複数レーンへ表示する例。
- 出典矛盾が未解決の出来事。
- URL互換性を維持するID変更禁止例。

## 記載形式

各例には入力データ、期待する意味、期待する表示、検証条件を記載します。

## 未決定事項

- 例をMarkdownだけで管理するか、テストfixtureと共有するか。
- 実在データを例に使う場合の最小引用範囲。
