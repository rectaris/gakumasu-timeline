{
  "name": "gakumasu-timeline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "validate:data": "node scripts/validate-data.mjs",
    "verify": "npm run validate:data && npm run test && npm run build",
    "build": "vite build",
    "deploy": "gh-pages -d dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^17.0.1",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "gh-pages": "^6.3.0",
    "playwright": "^1.59.1",
    "vite": "^7.2.4",
    "vitest": "^3.2.4"
  }
}
